# نظام الموارد البشرية الذكي 🚀

<div dir="rtl">

## نظرة عامة

نظام الموارد البشرية الذكي هو نظام متكامل يعتمد على الذكاء الاصطناعي لإدارة الموارد البشرية في الشركات. يوفر النظام مجموعة شاملة من الميزات بما في ذلك التنبؤ بالترقيات، تحليل الأداء، إدارة السياسات، وتوصيات الإجازات.

### المميزات الرئيسية ✨

- 🤖 **التنبؤ بالترقيات**: استخدام نماذج التعلم الآلي للتنبؤ بأهلية الموظفين للترقية
- 📊 **تحليل الأداء**: تحليل شامل لأداء الموظفين مع توصيات للتحسين
- 📋 **إدارة السياسات**: تحميل وإدارة والبحث في سياسات الشركة
- 🏖️ **توصيات الإجازات**: حساب الإجازات المستحقة وتقديم توصيات ذكية
- ✅ **فحص الامتثال**: التحقق من امتثال بيانات الموظفين للوائح العمل
- 🌐 **دعم اللغة العربية**: واجهة كاملة باللغة العربية والإنجليزية
- 🐳 **جاهز للنشر**: دعم كامل لـ Docker و Docker Compose
- 🗄️ **اتصال SQL Server**: دعم كامل للاتصال بقاعدة بيانات SQL Server والتدريب منها
- 📊 **لوحة تحكم احترافية**: لوحة تحكم ويب شاملة مع 8 صفحات رئيسية
- 📁 **دعم جميع صيغ الملفات**: CSV, Excel, JSON, Parquet, TSV, وغيرها

### التقنيات المستخدمة 🛠️

- **FastAPI**: إطار عمل حديث وسريع لبناء واجهات برمجة التطبيقات
- **scikit-learn**: مكتبة التعلم الآلي (RandomForest, GradientBoosting)
- **Pandas & NumPy**: معالجة وتحليل البيانات
- **Pydantic**: التحقق من صحة البيانات
- **Loguru**: نظام سجلات متقدم
- **Docker**: للنشر والتوزيع
- **SQL Server**: دعم الاتصال بقاعدة البيانات (pyodbc, pymssql, SQLAlchemy)
- **Bootstrap 5**: تصميم واجهة المستخدم
- **Chart.js**: الرسوم البيانية التفاعلية

---

## التثبيت والإعداد 📦

### المتطلبات الأساسية

- Python 3.11 أو أحدث
- pip (مدير حزم Python)
- Docker و Docker Compose (اختياري، للنشر)

### التثبيت المحلي

#### 1. استنساخ المشروع

```bash
git clone <repository-url>
cd hr-model
```

#### 2. إنشاء بيئة افتراضية

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

#### 3. تثبيت المتطلبات

```bash
pip install -r requirements.txt
```

#### 4. تشغيل التطبيق

```bash
python run.py
```

سيعمل التطبيق على: `http://localhost:8000`

الوثائق التفاعلية: `http://localhost:8000/docs`

---

## النشر باستخدام Docker 🐳

### البناء والتشغيل

#### استخدام Docker Compose (موصى به)

```bash
# بناء وتشغيل الحاوية
docker-compose up -d

# عرض السجلات
docker-compose logs -f

# إيقاف الحاوية
docker-compose down
```

#### استخدام Docker مباشرة

```bash
# بناء الصورة
docker build -t hr-ml-system .

# تشغيل الحاوية
docker run -d \
  -p 8000:8000 \
  -v $(pwd)/data:/app/data \
  -v $(pwd)/models:/app/models \
  -v $(pwd)/logs:/app/logs \
  -v $(pwd)/policies:/app/policies \
  --name hr-system \
  hr-ml-system

# عرض السجلات
docker logs -f hr-system

# إيقاف الحاوية
docker stop hr-system
docker rm hr-system
```

---

## 📊 لوحة التحكم - Dashboard

تم إضافة لوحة تحكم احترافية شاملة لإدارة نظام HR-ML بشكل مرئي وسهل.

### الوصول إلى لوحة التحكم

```
http://localhost:1234/static/dashboard/index.html
```

### الصفحات المتاحة

1. **🏠 الرئيسية**: إحصائيات عامة ورسوم بيانية لأداء النماذج
2. **📤 رفع البيانات**: واجهة سهلة لرفع ملفات البيانات
3. **🗄️ قاعدة البيانات**: ربط واختبار الاتصال بـ SQL Server
4. **🧠 التدريب**: تدريب النماذج وعرض النتائج
5. **🔮 التنبؤ**: إدخال بيانات موظف والحصول على تنبؤ
6. **📦 النماذج**: إدارة النماذج المحفوظة
7. **📈 التقارير**: تقارير مفصلة ورسوم بيانية تفاعلية
8. **⚙️ الإعدادات**: إعدادات النظام واللغة

### المميزات

- ✅ دعم كامل للغتين العربية والإنجليزية
- ✅ تصميم احترافي responsive
- ✅ رسوم بيانية تفاعلية (Chart.js)
- ✅ اتصال مباشر بجميع API endpoints
- ✅ تجربة مستخدم ممتازة

**للمزيد من التفاصيل، راجع ملف [DASHBOARD_GUIDE.md](DASHBOARD_GUIDE.md)**

---

## دليل الاستخدام 📖

### 1. رفع البيانات

قم برفع ملف بيانات الموظفين بأي من الصيغ المدعومة:

**الصيغ المدعومة:**
- 📊 **CSV**: `.csv`, `.tsv` (ملفات القيم المفصولة بفواصل)
- 📈 **Excel**: `.xlsx`, `.xls`, `.xlsb`, `.xlsm` (جميع إصدارات Excel)
- 📄 **Text**: `.txt` (ملفات نصية)
- 🔤 **JSON**: `.json` (ملفات JSON)
- 📦 **Parquet**: `.parquet` (تنسيق عمودي عالي الأداء)
- 🪶 **Feather**: `.feather` (تنسيق سريع للبيانات)

**مثال - رفع ملف CSV:**
```bash
curl -X POST "http://localhost:1234/upload/dataset?lang=ar" \
  -H "Content-Type: multipart/form-data" \
  -F "file=@employees.csv"
```

**مثال - رفع ملف Excel:**
```bash
curl -X POST "http://localhost:1234/upload/dataset?lang=ar" \
  -H "Content-Type: multipart/form-data" \
  -F "file=@employees.xlsx"
```

**مثال - رفع ملف XLSB:**
```bash
curl -X POST "http://localhost:1234/upload/dataset?lang=ar" \
  -H "Content-Type: multipart/form-data" \
  -F "file=@employees.xlsb"
```

**الأعمدة المطلوبة في الملف:**
- `experience`: سنوات الخبرة (0-60)
- `education_level`: المستوى التعليمي (0-10)
- `performance_score`: درجة الأداء (0-100)
- `training_hours`: ساعات التدريب (0-500)
- `awards`: عدد الجوائز (0-20)
- `avg_work_hours`: متوسط ساعات العمل اليومية (0-24)
- `department`: القسم (it, hr, sales, finance, marketing, operations)
- `gender`: الجنس (male, female)
- `promotion_eligible`: أهلية الترقية (0 أو 1) - للتدريب فقط

### 2. تدريب النموذج

بعد رفع البيانات، قم بتدريب نموذج التعلم الآلي:

```bash
curl -X POST "http://localhost:8000/train/?lang=ar" \
  -H "Content-Type: application/json" \
  -d '{
    "model_type": "random_forest",
    "use_cross_validation": true,
    "test_size": 0.2
  }'
```

**خيارات التدريب:**
- `model_type`: نوع النموذج (`random_forest` أو `gradient_boosting`)
- `use_cross_validation`: استخدام التحقق المتقاطع (true/false)
- `test_size`: نسبة بيانات الاختبار (0.1-0.4)

### 3. التنبؤ بالترقية

#### تنبؤ لموظف واحد

```bash
curl -X POST "http://localhost:8000/predict/?lang=ar" \
  -H "Content-Type: application/json" \
  -d '{
    "experience": 5.0,
    "education_level": 7,
    "performance_score": 85.0,
    "training_hours": 40.0,
    "awards": 2,
    "avg_work_hours": 8.5,
    "department": "it",
    "gender": "male"
  }'
```

#### تنبؤ جماعي

```bash
curl -X POST "http://localhost:8000/predict/batch?lang=ar" \
  -H "Content-Type: application/json" \
  -d '{
    "employees": [
      {
        "experience": 5.0,
        "education_level": 7,
        "performance_score": 85.0,
        "training_hours": 40.0,
        "awards": 2,
        "avg_work_hours": 8.5,
        "department": "it",
        "gender": "male"
      },
      {
        "experience": 3.0,
        "education_level": 6,
        "performance_score": 70.0,
        "training_hours": 25.0,
        "awards": 1,
        "avg_work_hours": 8.0,
        "department": "hr",
        "gender": "female"
      }
    ]
  }'
```

### 4. إدارة السياسات

#### رفع سياسة جديدة

```bash
curl -X POST "http://localhost:8000/policies/?lang=ar" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "سياسة الإجازات السنوية",
    "content": "يحق لكل موظف الحصول على 21 يوم إجازة سنوية مدفوعة الأجر...",
    "category": "leave",
    "tags": ["إجازات", "موارد بشرية"]
  }'
```

#### رفع ملف سياسة (PDF, DOCX, TXT)

```bash
curl -X POST "http://localhost:8000/policies/upload?title=سياسة الإجازات&category=leave&lang=ar" \
  -F "file=@policy.pdf"
```

#### البحث في السياسات

```bash
# البحث بالنص
curl "http://localhost:8000/policies/search/query?query=إجازة&lang=ar"

# البحث بالفئة
curl "http://localhost:8000/policies/search/query?category=leave&lang=ar"

# البحث بالوسوم
curl "http://localhost:8000/policies/search/query?tags=إجازات,موارد بشرية&lang=ar"
```

### 5. تحليل الأداء

```bash
curl -X POST "http://localhost:8000/hr/performance-analysis?lang=ar" \
  -H "Content-Type: application/json" \
  -d '{
    "performance_score": 85.0,
    "training_hours": 40.0,
    "awards": 2,
    "avg_work_hours": 8.5,
    "experience": 5.0
  }'
```

### 6. توصيات الإجازات

```bash
curl -X POST "http://localhost:8000/hr/leave-recommendation?lang=ar" \
  -H "Content-Type: application/json" \
  -d '{
    "employee_experience": 5.0,
    "current_leave_balance": 15,
    "department": "it",
    "performance_score": 85.0
  }'
```

### 7. فحص الامتثال

```bash
curl -X POST "http://localhost:8000/hr/compliance-check?lang=ar" \
  -H "Content-Type: application/json" \
  -d '{
    "employee_data": {
      "experience": 5.0,
      "education_level": 7,
      "training_hours": 40.0,
      "avg_work_hours": 8.5,
      "department": "it"
    }
  }'
```

### 8. فحص صحة النظام

```bash
# فحص بسيط
curl "http://localhost:8000/health/?lang=ar"

# فحص مفصل
curl "http://localhost:8000/health/detailed?lang=ar"
```

---

## دليل استكشاف الأخطاء 🔧

### المشكلة: النموذج غير موجود

**الخطأ**: `"النموذج غير موجود. يرجى تدريب النموذج أولاً"`

**الحل**:
1. تأكد من رفع ملف البيانات أولاً عبر `/upload/dataset`
2. قم بتدريب النموذج عبر `/train/`
3. تحقق من وجود ملف `promotion_model.pkl` في مجلد `models/`

### المشكلة: خطأ في رفع الملف

**الخطأ**: `"نوع الملف غير صالح"`

**الحل**:
- تأكد من أن الملف بأحد الصيغ المدعومة: CSV, TSV, Excel (.xlsx, .xls, .xlsb, .xlsm), JSON, Parquet, Feather, TXT
- تحقق من أن الملف يحتوي على جميع الأعمدة المطلوبة
- تأكد من أن حجم الملف لا يتجاوز 50 ميجابايت
- تأكد من أن الملف غير تالف ويمكن فتحه

### المشكلة: خطأ في التحقق من البيانات

**الخطأ**: `"بيانات الإدخال غير صالحة"`

**الحل**:
- تحقق من أن جميع القيم ضمن النطاقات المسموحة:
  - `experience`: 0-60
  - `education_level`: 0-10
  - `performance_score`: 0-100
  - `training_hours`: 0-500
  - `awards`: 0-20
  - `avg_work_hours`: 0-24
- تأكد من أن `department` و `gender` من القيم المسموحة

### المشكلة: Docker لا يعمل

**الحل**:
```bash
# تحقق من تشغيل Docker
docker --version

# تحقق من السجلات
docker-compose logs -f

# إعادة بناء الحاوية
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

### المشكلة: المنفذ 8000 مستخدم

**الحل**:
```bash
# Windows
netstat -ano | findstr :8000
taskkill /PID <PID> /F

# Linux/Mac
lsof -i :8000
kill -9 <PID>

# أو استخدم منفذ آخر
uvicorn run:app --port 8001
```

---

## الأمان والأفضليات 🔒

### في بيئة الإنتاج

1. **تحديد CORS Origins**:
   ```python
   # في run.py
   allow_origins=["https://yourdomain.com"]
   ```

2. **استخدام HTTPS**:
   - استخدم reverse proxy مثل Nginx
   - قم بتثبيت شهادة SSL

3. **متغيرات البيئة**:
   - لا تضع معلومات حساسة في الكود
   - استخدم ملف `.env` لتخزين الإعدادات

4. **قاعدة البيانات**:
   - استخدم قاعدة بيانات حقيقية (PostgreSQL, MySQL)
   - بدلاً من ملفات JSON

5. **المصادقة والتفويض**:
   - أضف نظام مصادقة (JWT, OAuth2)
   - حدد صلاحيات المستخدمين

### النسخ الاحتياطي

```bash
# نسخ احتياطي للبيانات
tar -czf backup-$(date +%Y%m%d).tar.gz data/ models/ policies/

# استعادة النسخة الاحتياطية
tar -xzf backup-20240101.tar.gz
```

---

## التطوير والمساهمة 👨‍💻

### إعداد بيئة التطوير

```bash
# تثبيت المتطلبات مع أدوات التطوير
pip install -r requirements.txt
pip install pytest black flake8 mypy

# تشغيل الاختبارات
pytest

# تنسيق الكود
black .

# فحص الكود
flake8 .
```

### إضافة ميزة جديدة

1. أنشئ فرع جديد:
   ```bash
   git checkout -b feature/new-feature
   ```

2. قم بإجراء التغييرات

3. اختبر التغييرات:
   ```bash
   pytest
   ```

4. أرسل Pull Request

---

## الأسئلة الشائعة ❓

### هل يمكن استخدام النظام مع لغات أخرى؟

نعم، النظام يدعم العربية والإنجليزية. يمكن إضافة لغات أخرى بسهولة عبر تحديث ملف `app/i18n.py`.

### هل يمكن تخصيص نموذج التعلم الآلي؟

نعم، يمكنك:
- اختيار نوع النموذج (RandomForest أو GradientBoosting)
- تعديل معاملات النموذج في `app/model_utils.py`
- إضافة ميزات جديدة للتدريب

### هل يدعم النظام قواعد بيانات؟

حالياً يستخدم النظام ملفات JSON و CSV. يمكن بسهولة إضافة دعم لقواعد البيانات مثل PostgreSQL أو MongoDB.

### كيف أضيف مستخدمين ومصادقة؟

يمكنك إضافة نظام مصادقة باستخدام:
- FastAPI Security utilities
- JWT tokens
- OAuth2

مثال:
```python
from fastapi.security import OAuth2PasswordBearer
oauth2_scheme = OAuth2PasswordBearer(tokenUrl="token")
```

---

## الترخيص 📄

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

---

## الدعم والتواصل 📧

للأسئلة والدعم:
- افتح Issue على GitHub
- راسلنا عبر البريد الإلكتروني

---

## الشكر والتقدير 🙏

شكراً لاستخدام نظام الموارد البشرية الذكي!

تم البناء باستخدام:
- [FastAPI](https://fastapi.tiangolo.com/)
- [scikit-learn](https://scikit-learn.org/)
- [Pandas](https://pandas.pydata.org/)
- [Pydantic](https://pydantic-docs.helpmanual.io/)

---

## الإصدارات 📋

### الإصدار 1.0.0 (الحالي)

- ✅ التنبؤ بالترقيات باستخدام التعلم الآلي
- ✅ تحليل الأداء
- ✅ إدارة السياسات
- ✅ توصيات الإجازات
- ✅ فحص الامتثال
- ✅ دعم اللغة العربية والإنجليزية
- ✅ دعم Docker
- ✅ واجهة API شاملة

### خطط مستقبلية

- 🔄 إضافة قاعدة بيانات
- 🔄 نظام المصادقة والتفويض
- 🔄 لوحة تحكم ويب
- 🔄 تقارير PDF
- 🔄 إشعارات البريد الإلكتروني
- 🔄 تكامل مع أنظمة HR الأخرى

---

</div>

## تكامل Cog 🐳

النظام يدعم الآن **Cog** من Replicate لتسهيل النشر:

### البدء السريع مع Cog

```bash
# بناء صورة Cog
cog build -t hr-ml-model

# اختبار التنبؤ
cog predict -i experience=5.0 -i education_level=7 -i performance_score=85.0 -i training_hours=40.0 -i awards=2 -i avg_work_hours=8.5 -i department="it" -i gender="male" -i language="ar"

# تشغيل خادم HTTP
cog run -p 5000

# النشر على Replicate
cog push r8.im/your-username/hr-ml-model
```

### استخدام السكريبت المساعد

```bash
# Linux/Mac
chmod +x cog_helper.sh
./cog_helper.sh check    # فحص المتطلبات
./cog_helper.sh build    # بناء الصورة
./cog_helper.sh test     # اختبار التنبؤ
./cog_helper.sh run      # تشغيل الخادم

# Windows
cog_helper.bat check
cog_helper.bat build
cog_helper.bat test
cog_helper.bat run
```

للمزيد من التفاصيل، راجع [COG_GUIDE.md](./COG_GUIDE.md)

---

## English Summary

**Smart HR System** - An AI-powered comprehensive HR management system with:
- ML-based promotion prediction
- Performance analysis
- Policy management
- Leave recommendations
- Compliance checking
- Full Arabic & English support
- Docker-ready deployment
- **Cog integration for easy deployment**

**Quick Start:**
```bash
pip install -r requirements.txt
python run.py
# Visit http://localhost:8000/docs
```

**Docker:**
```bash
docker-compose up -d
```

**Cog:**
```bash
cog build -t hr-ml-model
cog run -p 5000
```

For detailed documentation in Arabic, see above.
For Cog integration guide, see [COG_GUIDE.md](./COG_GUIDE.md).
---

## نقاط النهاية (API Endpoints) 🔗

### الصفحة الرئيسية
- `GET /` - معلومات النظام

### الفحص الصحي
- `GET /health/` - فحص صحة النظام
- `GET /health/detailed` - فحص صحة مفصل
- `GET /health/readiness` - فحص الجاهزية (Kubernetes)
- `GET /health/liveness` - فحص الحياة (Kubernetes)

### رفع البيانات
- `POST /upload/dataset` - رفع ملف بيانات الموظفين

### التدريب
- `POST /train/` - تدريب نموذج التعلم الآلي

### التنبؤ
- `POST /predict/` - التنبؤ لموظف واحد
- `POST /predict/batch` - التنبؤ لعدة موظفين

### السياسات
- `POST /policies/` - إنشاء سياسة جديدة
- `POST /policies/upload` - رفع ملف سياسة
- `GET /policies/` - الحصول على جميع السياسات
- `GET /policies/{policy_id}` - الحصول على سياسة محددة
- `PUT /policies/{policy_id}` - تحديث سياسة
- `DELETE /policies/{policy_id}` - حذف سياسة
- `GET /policies/search/query` - البحث في السياسات
- `GET /policies/statistics/summary` - إحصائيات السياسات

### عمليات الموارد البشرية
- `POST /hr/performance-analysis` - تحليل الأداء
- `POST /hr/leave-recommendation` - توصيات الإجازات
- `POST /hr/compliance-check` - فحص الامتثال
- `GET /hr/dashboard/summary` - لوحة معلومات الموارد البشرية

---

## هيكل المشروع 📁

```
hr-model/
├── app/
│   ├── config.py           # الإعدادات والتكوين
│   ├── data_utils.py       # أدوات معالجة البيانات
│   ├── model_utils.py      # أدوات التعلم الآلي
│   ├── i18n.py            # نظام الترجمة
│   └── policy_manager.py   # إدارة السياسات
├── routers/
│   ├── upload.py          # موجه رفع البيانات
│   ├── train.py           # موجه التدريب
│   ├── predict.py         # موجه التنبؤ
│   ├── policies.py        # موجه السياسات
│   ├── hr_operations.py   # موجه عمليات الموارد البشرية
│   └── health.py          # موجه الفحص الصحي
├── data/                  # مجلد البيانات
├── models/                # مجلد النماذج المدربة
├── logs/                  # مجلد السجلات
├── policies/              # مجلد السياسات
├── run.py                 # ملف التشغيل الرئيسي
├── requirements.txt       # المتطلبات
├── Dockerfile            # ملف Docker
├── docker-compose.yml    # ملف Docker Compose
└── README.md             # هذا الملف
```


